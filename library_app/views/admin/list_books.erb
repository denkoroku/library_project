<!-- Main Content will be dynamic -->
<div class="erb_content"
  <h1>List of Books </h1>

  <ul>
    <% @books.each do |book| %>
    <li><%= book.title %> by <%= book.author %></li>
    <% end %>
  </ul>
</div>
